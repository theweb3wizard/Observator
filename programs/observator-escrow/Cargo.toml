[package]
name = "observator-escrow"
version = "0.1.0"
description = "Observator conditional escrow — Solana/Anchor program (Phase 2)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "observator_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"
